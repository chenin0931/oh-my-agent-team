---
title: CLI command reference
description: One-page overview of every top-level OhMyAgentTeam CLI command. For full usage, run `ohmyagentteam <command> --help`.
---

import { Callout } from "fumadocs-ui/components/callout";

The OhMyAgentTeam CLI mirrors almost everything the Web UI can do (create [issues](/issues), assign [agents](/agents), start the [daemon](/daemon-runtimes), and more). This page lists every top-level command with a one-line description. For the full set of flags and examples, run `ohmyagentteam <command> --help`.

## Getting authenticated

Run this the first time you use the CLI to obtain a **personal access token (PAT)**:

```bash
omat login
```

Your browser opens automatically. After you approve in the web app, the CLI saves the PAT (prefixed with `mul_`) to `~/.ohmyagentteam/config.json`. Every subsequent command authenticates with that PAT.

<Callout type="tip">
For CI or headless environments, skip the browser flow: create a PAT in the web app under **Settings → Personal Access Tokens**, then run `omat login --token <mul_...>` to supply it directly.
</Callout>

For the difference between token types, see [Authentication and tokens](/auth-tokens).

## Auth and setup

| Command | Purpose |
|---|---|
| `omat login` | Log in and save a PAT |
| `omat auth status` | Show current login status, user, and workspace |
| `omat auth logout` | Clear the local PAT |
| `omat setup cloud` | One-shot setup for OhMyAgentTeam Cloud (login + install daemon) |
| `omat setup self-host` | One-shot setup for a self-hosted backend |

## Workspaces and members

| Command | Purpose |
|---|---|
| `omat workspace list` | List every workspace you can access |
| `omat workspace get <slug>` | Show details for one workspace |
| `omat workspace member list` | List members of the current workspace |
| `omat workspace update <id> --name "..." [--description "..."] [--context "..."] [--issue-prefix "..."]` | Update workspace metadata (admin/owner). Long fields accept `--description-stdin` / `--context-stdin`. |

## Issues and projects

<Callout type="info">
`list` commands (`omat issue list`, `autopilot list`, `project list`, etc.) print short, copy-paste-ready IDs by default — issue keys like `MUL-123` for issues, short UUID prefixes for the rest. The `<id>` argument on the follow-up commands below accepts either the short ID or the full UUID, so the typical flow is `omat issue list` → copy the key → `omat issue get MUL-123`. Pass `--full-id` to a list command when you need the canonical UUID.
</Callout>

| Command | Purpose |
|---|---|
| `omat issue list` | List issues (prints copy-paste-ready issue keys) |
| `omat issue get <id>` | Show a single issue (accepts an issue key or a UUID) |
| `omat issue create --title "..."` | Create a new issue |
| `omat issue update <id> ...` | Update an issue (status, priority, assignee, etc.) |
| `omat issue assign <id> --to <name>` | Assign to a member, agent, or squad (assigning to an agent triggers a run) |
| `omat issue status <id> <status>` | Shortcut to change status |
| `omat issue search <query>` | Keyword search |
| `omat issue children <id>` | List sub-issues grouped by stage |
| `omat issue pull-requests <id>` | List linked pull requests and their status |
| `omat issue runs <id>` | Show agent runs on an issue |
| `omat issue run-messages <task-id>` | Show messages for one execution |
| `omat issue usage <id>` | Show aggregated token usage for an issue |
| `omat issue rerun <id>` | Re-enqueue a fresh task for the issue's current agent assignee |
| `omat issue cancel-task <task-id>` | Cancel a queued or running task |
| `omat issue comment <id> ...` | Nested: view / post comments |
| `omat issue comment resolve/unresolve <comment-id>` | Mark a comment thread resolved or unresolved |
| `omat issue subscriber <id> ...` | Nested: subscribe / unsubscribe |
| `omat issue metadata <id> ...` | Nested: read / write issue metadata |
| `omat issue label <id> ...` | Nested: manage labels on an issue |
| `omat project list/get/create/update/delete/status` | Project CRUD |
| `omat label list/create/update/delete` | Workspace label CRUD |

## Agents and skills

| Command | Purpose |
|---|---|
| `omat agent list` | List the workspace's agents |
| `omat agent get <slug>` | Show an agent's configuration |
| `omat agent create ...` | Create an agent |
| `omat agent update <slug> ...` | Update an agent |
| `omat agent archive <slug>` | Archive |
| `omat agent restore <slug>` | Restore an archived agent |
| `omat agent tasks <slug>` | Show an agent's task history |
| `omat agent avatar <slug>` | Upload an agent avatar |
| `omat agent env <slug> ...` | Read or replace an agent's custom environment variables |
| `omat agent skills ...` | Nested: attach / detach skills |
| `omat skill list/get/create/update/delete` | Skill CRUD |
| `omat skill import ...` | Import a skill from GitHub, ClawHub, or the local machine |
| `omat skill files ...` | Nested: manage a skill's files |

### Skill import conflicts

`omat skill import --url <url>` defaults to `--on-conflict fail`. If a skill
with the same name already exists, the command exits with a structured
`conflict` result and does not change the workspace.

Use `--on-conflict overwrite` when you created the existing skill and want to
replace its content while preserving its ID and agent bindings. Use
`--on-conflict rename` to import a copy with an automatic suffix such as `-2`.
Use `--on-conflict skip` to leave the existing skill untouched and report
`skipped`.

```bash
omat skill import --url https://skills.sh/acme/repo/review-helper
omat skill import --url https://skills.sh/acme/repo/review-helper --on-conflict overwrite
omat skill import --url https://skills.sh/acme/repo/review-helper --on-conflict rename
omat skill import --url https://skills.sh/acme/repo/review-helper --on-conflict skip
```

## Squads

| Command | Purpose |
|---|---|
| `omat squad list` | List squads in the workspace |
| `omat squad get <id>` | Show a single squad |
| `omat squad create --name "..." --leader <agent>` | Create a squad (owner / admin) |
| `omat squad update <id> ...` | Update name, description, instructions, leader, or avatar |
| `omat squad delete <id>` | Archive (soft-delete) — transfers assigned issues to the leader |
| `omat squad member list/add/remove/set-role <squad-id>` | Manage squad members and update roles in place |
| `omat squad activity <issue-id> <action\|no_action\|failed> --reason "..."` | Used by squad leader agents to record an evaluation per turn |

See [Squads](/squads) for the full model.

## Autopilots

| Command | Purpose |
|---|---|
| `omat autopilot list` | List every autopilot in the workspace |
| `omat autopilot get <id>` | Show a single autopilot |
| `omat autopilot create ...` | Create an autopilot |
| `omat autopilot update <id> ...` | Update |
| `omat autopilot delete <id>` | Delete |
| `omat autopilot runs <id>` | Show run history |
| `omat autopilot trigger <id>` | Trigger a run manually |
| `omat autopilot trigger-add/update/delete <id>` | Manage schedule or webhook triggers |
| `omat autopilot trigger-rotate-url <id> <trigger-id>` | Rotate a webhook trigger URL |

`omat autopilot create/update` also accepts `--subscriber` to set default
subscribers for issues created by a `create_issue` autopilot; update accepts
`--clear-subscribers` to remove them.

## Daemon and runtimes

| Command | Purpose |
|---|---|
| `omat daemon start` | Start the daemon (background by default; add `--foreground` to run in the foreground) |
| `omat daemon stop` | Stop the daemon |
| `omat daemon restart` | Restart the daemon |
| `omat daemon status` | Check whether the daemon is online and its concurrency |
| `omat daemon logs` | View daemon logs |
| `omat runtime list` | List runtimes in the current workspace |
| `omat runtime usage` | Show resource usage |
| `omat runtime activity` | Recent activity log |
| `omat runtime update <id> ...` | Initiate a CLI update on a runtime |
| `omat runtime delete <id> [--cascade]` | Delete a runtime, optionally archiving bound agents |
| `omat runtime profile ...` | Manage custom runtime profiles and local path overrides |

## Miscellaneous

| Command | Purpose |
|---|---|
| `omat repo checkout <url>` | Clone a repo locally for agents to use |
| `omat config` | View or edit local CLI configuration |
| `omat version` | Print the CLI version |
| `omat update` | Upgrade the CLI to the latest release |
| `omat attachment download <id>` | Download an attachment from an issue or comment |

## Getting full flags

Every command supports `--help`:

```bash
omat issue create --help
omat agent update --help
```

v2 will ship a dedicated detailed reference page for each command.

## Next steps

- [Authentication and tokens](/auth-tokens) — PAT vs. JWT vs. daemon token
- [Daemon and runtimes](/daemon-runtimes) — how the `daemon` commands work under the hood
- [Creating and configuring agents](/agents-create) — all options for `omat agent create`
